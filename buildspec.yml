version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies (none needed for static site)"
  build:
    commands:
      - echo "Packaging files for deployment"
artifacts:
  files:
    - '**/*'
